<%= render 'partials/navbar' %>

<h1><%= @event.title %></h1>

<h3>Categories</h3>

<% if @event.categories.length === 0  %>
	No categories added to "<%= @event.title %>" yet.
	<br>
	
<% end %>

<% @event.categories.each do |category| %>

<% end %>

<br><br>

<div class="text-center">
	<a href="/category/new" class="btn btn-default">Add Category</a>
</div>
